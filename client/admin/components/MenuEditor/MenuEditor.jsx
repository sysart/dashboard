import React, { PropTypes } from 'react';

class MenuEditor extends React.Component {
    render() {
        return (
            <h1>menu editor...</h1>
        );
    }
}

export default MenuEditor;
